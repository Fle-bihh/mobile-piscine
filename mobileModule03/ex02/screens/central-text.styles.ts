import { StyleSheet } from "react-native";

export const getCentralTextScreenStyles = () => {
	return StyleSheet.create({
		container: {
			flex: 1,
			alignItems: "center",
			padding: 16,
			gap: 16,
		},
	});
};
